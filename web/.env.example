# Base URL of the Frontdesk API, including the /api prefix.
#
# Inlined into the browser bundle at build time, so it must be a public URL —
# never put the model API key or the database URL here.

NEXT_PUBLIC_API_URL="http://localhost:8080/api"
